if [ $# -eq 1 ]; then
    echo "running with port $1"
    FLASK_ENV=development flask run --host=0.0.0.0 --port=$1
else
    echo "running on default port 35687"
    FLASK_ENV=development flask run --host=0.0.0.0 --port=35687
fi
